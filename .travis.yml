language: go

go:
  - 1.12.x

# Only clone the most recent commit.
git:
  depth: 1

install: true

notifications:
  email: true

branches:
  only:
    - master

services:
  - docker

script:
  - go test -v ./...

after_success:
  - cd http-echo
  - make image